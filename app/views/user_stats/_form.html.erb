<%= form_with(model: user_stat, local: true) do |form| %>
  <% if user_stat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_stat.errors.count, "error") %> prohibited this user_stat from being saved:</h2>

      <ul>
      <% user_stat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :state %>
    <%= form.text_field :state, id: :user_stat_state %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :user_stat_email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
